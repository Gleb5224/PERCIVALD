  #include <iostream>
  using namespace std;
  int main()
{
      setlocale(0,".1251");
      double a,b;
      cout << "Введiть два числа (через пробiл)\n";
      cin >> a >> b;
      cout << "Сума цих чисел - "<< a + b << endl;
      return 0;
